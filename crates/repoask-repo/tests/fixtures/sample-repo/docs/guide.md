# Example Auth Kit

## API

### validateToken

Use `validateToken` to verify a JWT and return a session.

```ts
validateToken(token, secret)
```

## Usage

Refresh token usage is documented here.

```ts
demoLogin()
```
